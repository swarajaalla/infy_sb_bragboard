to run all services ( frontedna dn backend)
docker compose up -d --build (not working )
docker compose up db -d ( just run dataabased)


to down all services
docker compose down


manual 
# Terminal 1 - Backend
cd backend
uvicorn app.main:app --reload

# Terminal 2 - Frontend  
cd frontend
npm run dev


to kill ports : 
lsof -ti:8000 | xargs kill -9 2>/dev/null || echo "No process found on port 8000"

-----------------------
run 
step 1 : docker compose up db -d 

manual 
# Terminal 1 - Backend
cd backend
uvicorn app.main:app --reload

# Terminal 2 - Frontend  
cd frontend
npm run dev
